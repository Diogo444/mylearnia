#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Identifiants MySQL
const char user[]     = "root";         // 🔁 à modifier selon ton serveur
const char password[] = "root";         // 🔁 idem

// Objet WiFiClient global (nécessaire pour ESP32_MySQL)
WiFiClient client;

#endif
